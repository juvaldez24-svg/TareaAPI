# TareaAPI
Crear un endpoint ("/creditos") que regrese todos los créditos activos del JSON. Crear un endpoint ("/creditos/:importe") que regrese todos los créditos activos mayores al importe proporcionado. Crear un endpoint ("/creditos-cliente/:id") que regrese todos los créditos de un cliente.
